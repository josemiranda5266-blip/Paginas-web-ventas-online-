# PORT: The port the server binds to (defaults to 3000)
PORT=3000

# NODE_ENV: production or development
NODE_ENV=development

# JWT_SECRET: Secret key for signing authentication tokens
JWT_SECRET=super_secret_jwt_token_saas_ecommerce_key_2026

# APP_URL: Base URL of the SaaS platform
APP_URL=http://localhost:3000
